import { FormatValuesPipe } from './format-values.pipe';

describe('FormatValuesPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
